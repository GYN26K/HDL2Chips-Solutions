module not_gate(
    input in,
    output out
);
    // Write your code here
    // Hint: Use the ~ operator to invert the input
    // Think: out should be the opposite of in
    assign out = ~in;
    
endmodule